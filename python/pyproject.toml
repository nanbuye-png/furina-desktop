[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "furina-tools"
version = "0.1.3"
description = "Furina Agent Python tool sidecar (stdlib only)"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["."]
